import { definePageMetadata } from "../../../packages/makit/src/metadata/index.ts";

export default definePageMetadata({
  id: "guides.changelog",
  title: "Changelog pages",
  description: "Generate a changelog page from a GitHub repository's releases at build time.",
});
