import { definePageMetadata } from "../../../packages/makit/src/metadata/index.ts";

export default definePageMetadata({
  id: "reference.theme-packages",
  title: "Theme packages",
  description: "Build a theme package: entry points, components, and the manifest.",
});
