# Getting Started

Install Makit and run `makit init` to scaffold a new project:

```sh
npm create @natsuneko-laboratory/makit@latest
```

This creates a `docs/` directory, a `makit.config.ts`, and a starter page.
