---
title: Configuration
order: 1
---

# Configuration

Configure Makit via `makit.config.ts`. This page's title and sort order come from flat
front matter instead of a sibling `configuration.meta.ts` — a lightweight alternative for
pages that only need a couple of scalar overrides (spec §17).
