import { definePageMetadata } from "@natsuneko-laboratory/makit/metadata";

export default definePageMetadata({
  id: "guides.theming",
  title: "Replacing a component",
  description: "The convention directory, wrapping versus replacing, and client components.",
  taxonomy: { tags: ["guide"] },
});
