import { definePageMetadata } from "@natsuneko-laboratory/makit/metadata";

export default definePageMetadata({
  id: "guides.imports",
  title: "What a component can import",
  description: "Packages Makit resolves for your components, data loaders, and styling.",
  taxonomy: { tags: ["guide"] },
});
