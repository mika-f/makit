import { definePageMetadata } from "@natsuneko-laboratory/makit/metadata";

export default definePageMetadata({
  id: "index",
  title: "Makit Theme Example",
  description: "A site whose header, title block, footer, and theme toggle are custom.",
  taxonomy: { tags: ["example", "theming"] },
});
